const url = 'http://localhost:3000';
const URL_AUTH_SIGNIN = `${url}/auth/signin`;
const URL_AUTH_REFRESH = `${url}/auth/refresh`;
const URL_POSTS = `${url}/posts`;
const URL_CUSTOMERS = `${url}/customers`;

export { URL_AUTH_SIGNIN, URL_AUTH_REFRESH, URL_POSTS, URL_CUSTOMERS };
